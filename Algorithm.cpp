#include "Algorithm.h"


template<class T>
std::vector<T> Algorithm<T>::select(int k, std::vector<T> arr, int l , int r){
    if(r == -1){
        r = arr.size();
    }
    return nullptr;
}

template<class T>
void Algorithm<T>::partition(std::vector<T> arr,int x ,int l , int r){
    if(r == -1){
            r = arr.size();
    }
}
